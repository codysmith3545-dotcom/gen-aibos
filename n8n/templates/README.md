# n8n Templates

Every n8n workflow must have a workflow intake spec, JSON export, short README, logging to `workflow_runs`, approval handling if risky, and a `system_status` check before external actions.

Initial workflows to build: Daily Business Summary, Gmail Lead Intake in shadow mode, Google Drive File Classifier in shadow mode, Approval Request Notifier, Failed Workflow Alert, n8n Export to GitHub.
