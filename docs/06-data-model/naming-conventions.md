# Naming Conventions

Use plural snake_case table names. Use UUID primary keys unless there is a specific reason not to. Important tables include `created_at` and `updated_at`. Source-derived records should include source, pulled_at, last_verified_at, confidence, and verification status.
