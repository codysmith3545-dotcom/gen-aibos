# ADR-0002 — Use GitHub as Change Control

Use GitHub as the source of truth for code, SOPs, schemas, migrations, prompts, and policies because Claude Code and Codex work naturally through repos, branches, PRs, issues, tests, and docs.
