# ADR-0004 — Use Approval Requests for Risky Actions

Risky actions must create `approval_requests` because AI can prepare and recommend, but humans must control money, legal commitments, client-facing actions, production writes, and destructive changes.
